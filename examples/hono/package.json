{
  "name": "vite-ssr-components-examples-hono",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "$npm_execpath run build && vite preview",
    "deploy": "$npm_execpath run build && wrangler deploy"
  },
  "dependencies": {
    "hono": "^4.7.10",
    "vite-ssr-components": "file:../.."
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.2.3",
    "vite": "^8.0.10",
    "wrangler": "^4.16.0"
  }
}
